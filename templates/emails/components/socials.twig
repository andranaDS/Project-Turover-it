<menu>
    <item href="{{ 'components.socials.linkedin'|trans({}, 'emails') }}">
        <img src="{{ email.image('@images/email/logo-linkedin.png') }}"
             class="social" alt="LinkedIn">
    </item>
    <item href="{{ 'components.socials.twitter'|trans({}, 'emails') }}">
        <img src="{{ email.image('@images/email/logo-twitter.png') }}"
             class="social" alt="Twitter">
    </item>
    <item href="{{ 'components.socials.facebook'|trans({}, 'emails') }}">
        <img src="{{ email.image('@images/email/logo-facebook.png') }}"
             class="social" alt="Facebook">
    </item>
</menu>
